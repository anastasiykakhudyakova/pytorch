'''Задание
Создайте нейросеть со следующей архитектурой: 5 нейронов на входе, три скрытых слоя, 1 нейрон на выходе (бинарная классификация).
При этом каждый скрытый слой должен пропускаться через различных функции активации.
Подумайте, какая должна быть функция активация на выходе в предыдущем задании, с учётом того, что это бинарная классификация. Добавьте её.
Попробуйте поменять оптимизатор. Какой лучше работает в Вашем случае: Adam или SGD?'''
import torch #Основные операции-Библиотека PyTorch - основная библиотека для глубокого обучения Работу с тензорами (многомерные массивы, похожие на NumPy),Автоматическое дифференцирование (вычисление градиентов),GPU-ускорение вычислений,Создание и обучение нейронных сетей
import torch.nn as nn #Слои и архитектура-Подмодуль PyTorch - nn (neural networks),Готовые слои нейронных сетей (Linear, Conv2d, LSTM и т.д.),,Функции активации (ReLU, Sigmoid, Tanh),Функции потерь (MSE, CrossEntropy, BCELoss),Контейнеры для построения архитектуры
import torch.optim as optim #Оптимизаторы(обучают сеть)-подмодуль PyTorch - optim (optimization algorithms),Различные оптимизаторы для обучения,Алгоритмы обновления весов нейросети
import numpy as np #Математические операции-NumPy - библиотека для научных вычислений,Мощные N-мерные массивы (ndarray),Математические функции для работы с массивам,Линейную алгебру, статистику, преобразования Фурье

# Генерируем случайные данные
X = torch.randn(100, 5)  # (100 образцов, 5 признаков) torch.randn - генерирует тензор со случайными числами из нормального распределения (среднее=0, дисперсия=1)
y = torch.randint(0, 2, (100, 1)).float()  # Метки классов (0 или 1)
# torch.randint(0, 2, (100, 1)) - генерирует случайные целые числа от 0 до 1 (2 не включается)
# .float() - преобразует целые числа в тип float (требуется для бинарной кросс-энтропии)
# Результат: тензор формы (100, 1) с нулями и единицами

# Тестовые данные
X_test = torch.randn(10, 5)  # 10 тестовых образцов, 5 признаков
y_test = torch.randint(0, 2, (10, 1)).float()  # Метки для тестовых данных

# Определяем архитектуру нейросети
class NeuralNetwork(nn.Module):  # Наследуемся от базового класса nn.Module
    def __init__(self):  # Конструктор класса
        super(NeuralNetwork, self).__init__()  # Вызываем конструктор родительского класса-Это необходимо для правильной инициализации нейросети
        
        self.fc1 = nn.Linear(5, 10)  #(Первый скрытый слой: 5 входов → 10 нейронов) nn.Linear - полносвязный слой (линейное преобразование),Аргументы: (in_features, out_features)
        self.fc2 = nn.Linear(10, 8)  #(Второй скрытый слой: 10 входов → 8 нейронов)
        self.fc3 = nn.Linear(8, 6)   #(Третий скрытый слой: 8 входов → 6 нейронов) 
        self.fc_out = nn.Linear(6, 1)  #(Выходной слой: 6 входов → 1 нейрон) Один выходной нейрон для бинарной классификации

    def forward(self, x):  # Метод прямого распространения
        # x - входные данные формы (batch_size, 5)
        
        x = torch.relu(self.fc1(x))  # ReLU для первого скрытого слоя - функция активации ReLU: f(x) = max(0, x)- не затухает градиент для положительных значений, быстро вычисляется
        x = torch.tanh(self.fc2(x))  # Tanh для второго скрытого слоя - гиперболический тангенс: f(x) = (e^x - e^(-x)) / (e^x + e^(-x))- Диапазон значений: от -1 до 1, центрирует данные (среднее около 0)
        x = torch..relu(self.fc3(x))  # ReLU для третьего скрытого
        x = torch.sigmoid(self.fc_out(x))  # Sigmoid на выходном слое - сигмоида: f(x) = 1 / (1 + e^(-x))-Сигмоида на выходе преобразует значения в вероятность от 0 до 1- Это необходимо для бинарной классификации 
        return x  # Возвращаем предсказания (вероятности)

model = NeuralNetwork()  # Создаем объект нейросети
criterion = nn.BCELoss()  # Функция потерь-Binary Cross Entropy Loss (бинарная кросс-энтропия)-Подходит для бинарной классификации с выходной сигмоидой

# Оптимизаторы для сравнения
optimizers = {
    'Adam': optim.Adam(model.parameters(), lr=0.001), # Adam - адаптивный оптимизатор, комбинирует идеи Momentum и RMSprop, model.parameters() - все обучаемые веса модели 
    'SGD': optim.SGD(model.parameters(), lr=0.001)    # SGD - стохастический градиентный спуск - Обновляет веса в направлении антиградиента
}

# Обучение модели с разными оптимизаторами
for opt_name, optimizer in optimizers.items():  # Перебираем оба оптимизатора - opt_name - имя оптимизатора ('Adam' или 'SGD'), optimizer - объект оптимизатора
    print(f"\nОбучение с оптимизатором: {opt_name}")
    
    #сброс весов
    def reset_weights(m):  # Определяем функцию для сброса весов слоя
        if isinstance(m, nn.Linear):  # Проверяем, является ли слой полносвязным
            nn.init.xavier_uniform_(m.weight)  # Инициализация весов по методу Xavier - xavier_uniform - равномерное распределение с дисперсией 2/(in+out)
            if m.bias is not None:  # Если у слоя есть смещение (bias)
                nn.init.constant_(m.bias, 0)  # Обнуляем смещение
    
    model.apply(reset_weights)  # Применяем сброс весов ко всем слоям модели, model.apply() - рекурсивно применяет функцию к каждому модулю сети
    
    # Обучение в течение 100 эпох
    for epoch in range(100):  # epoch - номер текущей эпохи (от 0 до 99)
        optimizer.zero_grad()  # Обнуляем градиенты оптимизатора - Накопление градиентов по умолчанию включено, поэтому их нужно обнулять     
        outputs = model(X)  # Прямой проход: получаем предсказания на обучающих данных (X - входные данные, outputs - предсказанные вероятности)
        loss = criterion(outputs, y)  #  criterion - функция потерь (BCELoss) - Вычисляем ошибку между предсказаниями и истинными метками
        loss.backward()  # Обратное распространение ошибки - Вычисляет градиенты loss по всем параметрам сети (автоматическое дифференцирование)
        optimizer.step()  # Обновляем веса модели - Оптимизатор делает шаг в направлении антиградиента
        
        # Выводим информацию каждые 10 эпох
        if (epoch + 1) % 10 == 0:  # epoch+1 - текущая эпоха (для вывода с 1)
            print(f'Epoch [{epoch+1}/100], Loss: {loss.item():.4f}') # loss.item() - извлекает значение тензора loss как число Python, .4f - формат вывода: 4 знака после запятой
    
    # Проверка на тестовых данных
    with torch.no_grad():  # Отключаем вычисление градиентов (экономит память и ускоряет)
        test_outputs = model(X_test)  # Получаем предсказания для тестовых данных - torch.no_grad() - контекстный менеджер, в котором не отслеживается история градиентов
        predicted = (test_outputs > 0.5).float()  # Преобразуем вероятности в классы - test_outputs > 0.5 - логический тензор (True для предсказанных единиц), .float() - преобразует True→1.0, False→0.0
        accuracy = (predicted == y_test).float().mean()  # Вычисляем точность, predicted == y_test - сравнение предсказаний с истинными метками, .mean() - среднее значение (доля правильных ответов)
        print(f'Точность на тестовых данных: {accuracy.item() * 100:.2f}%') # accuracy.item() * 100 - переводим долю в проценты
    
